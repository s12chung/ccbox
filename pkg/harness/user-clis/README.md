Drop-in CLIs live here as `<name>/CLI.yaml` (+ optional `<name>/config/`), the same format as the built-ins.

Look there for examples: https://github.com/s12chung/ccbox/tree/main/pkg/harness/clis
Names conflicting with built-in clis are skipped with a warning.
