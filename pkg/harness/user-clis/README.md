Drop-in CLIs live here as `<name>/CLI.yaml` (+ optional `<name>/config/`), the same format as the built-ins.

Look there for examples: https://github.com/s12chung/ccbox/tree/main/pkg/harness/clis. Your custom CLIs override the built-ins.
