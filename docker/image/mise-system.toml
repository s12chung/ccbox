# System toolchain — shared by all users (COPY'd to /etc/mise/config.toml).

[tools]
# Language runtimes
node   = "26.3.0"
# postinstall re-adds setuptools/wheel CPython 3.12+ dropped (setuptools = 3.13's distutils shim)
python = { version = "3.13", postinstall = "python -m pip install --no-cache-dir setuptools==82.0.1 wheel==0.47.0" }
ruby   = "3.4"
go     = "1.26"

# Dev-workflow CLIs
ripgrep = "15.1.0"
jq      = "1.8.1"
yq      = "4.53.3"

# Test + lint tooling
shellcheck    = "0.11.0"
hadolint      = "2.14.0"
bats          = "1.13.0"
golangci-lint = "2.13.1"

# Installs a lot of version independent apt-get packages
[tools."npm:playwright"]
version     = "latest"
# Chromium system libs (no browser binaries) + the browser itself, into
# PLAYWRIGHT_BROWSERS_PATH (see Dockerfile) so the whole image shares one copy
postinstall = "playwright install-deps chromium && playwright install chromium"